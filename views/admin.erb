<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
  <div class = "container">
    <div class="row top">
      <div class="col-md-offset-11">
        <a href="/">
          <button name="button">Back</button>
        </a>
      </div>
    <div class="row">
      <div class="col-md-offset-5">
        <ul>

        </ul>
      </div>
    </div>
    </div>
    <div class = "row" >
      <% tshirts.each do |tshirt| %>
      <div class="col-md-4">
        <div id="name">
          <img width=100 height=120 src="<%= tshirt.img_url%>"/>
          <h3><%= tshirt.name%></h3>
          <h3>$<%= tshirt.price%></h3>
          <p><%= tshirt.quantity%> shirts left</p>
          <form id="form_id" action="/admin" method="POST">
            <input type="hidden" name="id" value="<%= tshirt.id %>"/>
            <input type="hidden" name="_method" value="PUT"/>
            <input type="text" name="quantity" placeholder="Enter New Quantity"/>
            <button name="submit">GO!</button>
          </form>
        </div>
      </div>
      <% end %>
    </div>

    <div class="row bottom">
      <div id="purchase history">
        <h2>Purchase History</h2>
        <% purchases.each do |purchase| %>
          <p><%= purchase.id %>. <%= purchase.name %>, <%= purchase.email %>,
            <%= purchase.name %>, <%= purchase.tshirt_id %>,
            <%= purchase.quantity %> shirt(s) purchased, <%= purchase.created_at %></p>
        <% end %>
      </div>
    </div>

  </div>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
</body>
</html>
